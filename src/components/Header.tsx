import React from "react";
import styled from "@emotion/styled";
import { jsx } from "@emotion/react";

const StyledHeader = styled.h1`
  color: #4064ac;
  margin-bottom: 16px;
`;

const Header = () => <StyledHeader>Dictionary Search</StyledHeader>;

export default Header;
